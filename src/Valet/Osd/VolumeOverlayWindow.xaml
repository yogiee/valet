<Window x:Class="Valet.Osd.VolumeOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        SizeToContent="Manual"
        Width="240"
        Height="50"
        Opacity="0">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI" />
            <Setter Property="Foreground" Value="#FFFFFFFF" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Border CornerRadius="10"
            Background="#CC1C1C1C"
            BorderBrush="#33FFFFFF"
            BorderThickness="1"
            Padding="12,8,12,8">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="0" Opacity="0.5" />
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Icon (Segoe Fluent Icons / Segoe MDL2 Assets). Codepoints flip on mute. -->
            <TextBlock x:Name="IconGlyph"
                       Grid.Column="0"
                       FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                       FontSize="18"
                       Text="&#xE767;"
                       Margin="0,0,10,0" />

            <!-- Bar: track + fill -->
            <Grid Grid.Column="1" Height="5" VerticalAlignment="Center">
                <Rectangle RadiusX="2.5" RadiusY="2.5" Fill="#33FFFFFF" />
                <Rectangle x:Name="BarFill"
                           HorizontalAlignment="Left"
                           Width="0"
                           RadiusX="2.5" RadiusY="2.5"
                           Fill="#FFFFFFFF" />
            </Grid>

            <!-- Numeric label (dB suffix stripped in code-behind) -->
            <TextBlock x:Name="LabelText"
                       Grid.Column="2"
                       Margin="10,0,0,0"
                       FontSize="20"
                       MinWidth="50"
                       TextAlignment="Right"
                       Text="—" />
        </Grid>
    </Border>
</Window>
